<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.uniovi.eii.voluntacovid">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.DetalleAyudaCompletadaVoluntario"></activity>
        <activity android:name=".activities.DetalleAyudaProcesoVoluntario" />
        <activity android:name=".activities.DetalleAyudaCompletada" />
        <activity android:name=".activities.DetalleAyudaAsignada" />
        <activity android:name=".activities.DetalleAyudaPendienteVoluntario" />
        <activity android:name=".activities.DetalleAyudaPendiente" />
        <activity android:name=".activities.VoluntarioActivity" />
        <activity android:name=".activities.InicioSesionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.RegistroActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".activities.SolicitarAyudaActivity" />
        <activity android:name=".activities.NecesitadoActivity" />
        <activity android:name=".activities.PaginaPrincipalActivity" />
    </application>

</manifest>